[project]
name = "coding-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "mlx-vlm>=0.1.15",
    "python-dotenv>=1.2.1",
    "pyyaml>=6.0",
]
